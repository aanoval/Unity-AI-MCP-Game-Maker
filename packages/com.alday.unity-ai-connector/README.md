# Unity AI Connector Package

Editor-only Unity package for local AI automation.

Menu items:

```text
Tools > Unity AI Connector > Start Local Server
Tools > Unity AI Connector > Stop Local Server
Tools > Unity AI Connector > Print Token
```

The generated config is stored in:

```text
UserSettings/UnityAiConnector.json
```
